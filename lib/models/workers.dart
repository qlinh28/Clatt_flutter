class Worker {
  String name, image, address, introduce;
  int price;

  Worker({
    required this.name,
    required this.image,
    required this.address,
    required this.introduce,
    required this.price,
  });
}
